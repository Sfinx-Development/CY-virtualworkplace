import { Container } from "@mui/material";

export default function Office() {
  return (
    <Container sx={{ padding: "20px" }}>
      <div
        style={{
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
        }}
      >
        OFFICE ska vara generiskt 
      </div>
    </Container>
  );
}
